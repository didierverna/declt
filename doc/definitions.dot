strict digraph
{
  margin = 0;
  rankdir = TD;
  node [ shape = box color = blue ]

  { rank = same "component" "package" "symbol" }

  "definition" -> "component";
  "definition" -> "package";
  "definition" -> "symbol";
}
